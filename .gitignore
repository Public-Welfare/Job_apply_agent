# Dependencies
node_modules/

# Secrets & local env — NEVER commit
.env
.env.local
.env.*.local

# Runtime data (regenerated; may contain personal info or an API token)
data/applications.db
data/applications.db-shm
data/applications.db-wal
data/crawl_state.json
data/crawl_state.json.migrated
data/llm.json
data/llm.json.migrated
data/applications.json
data/applications.json.migrated

# Generated resumes (contain personal info; recreated at runtime)
resumes/

# LaTeX build artifacts
*.aux
*.out
docs/*.pdf
docs/*.aux
docs/*.log

# React build output (built in Docker / via `npm run build:ui`)
web/static/dist/

# OS / editor cruft
.DS_Store
Thumbs.db
